# TSS Clipboard todo list:

 - small clipboard app that stays hidden by default
 - starts on pc start or through a bat/exe file
 - access via a shortcut too such as ALT + C 
 - small box appears (styled like a fallout terminal (see my discord theme for styles etc to match))
 - list of entries upto 100
 - rich click entry to edit it
 - left click to copy it to windows clipboard so we can CTRL + V it elsewhere
 - little text box at top to paste in content and a save button to save that entry.
 - must be able to move window around while open
 - we do not need to see the full entry just keep it simple to see what it starts with
 - must be a app not a website
 